version X -> Y

1. cp -r AI/MMAI/BAI/v{X,Y}

2. cp -r AI/MMAI/schema/v{X,Y}

3. Edit MMAI_FILES in AI/MMAI/CMakeLists.txt by adding the new files
  (copy the two vX file sections and replace paths to be for vY)


4. Text replacements: replace all occurrances of
  - "/vX/" -> "/vY/"
  - "::VX" -> "::VY"
  ^ in AI/MMAI/BAI/vY/
  ^ in AI/MMAI/schema/vY/

5. Edit AI/MMAI/BAI/base.cpp and add #include + case stmt for version Y
   Edit AI/MMAI/BAI/model/TorchModel.cpp and add case stmt for version Y (2 places)
   Edit AI/MMAI/schema/schema.h add #include for version Y

6. cp ML/user_agents/agent-v{X,Y}.cpp
   cp ML/user_agents/agent-v{X,Y}.h

7. Edit the "add_executable(mlclient-cli" line in ML/CMakeLists.txt
  (add vY agents)

8. Edit ML/user_agents/agent-vY.cpp and ML/user_agents/agent-vY.h
  (replace VX with VY and replace some integers X with Y)

9. Edit ML/main.cpp add #include for vY + replace "AgentVX" with "AgentVY"

build & test

$ rel/bin/mlclient-cli --headless --map gym/generated/evaluation/8x64.vmap \
  --left-ai MMAI_USER --right-ai StupidAI --loglevel-ai error --random-heroes 1
